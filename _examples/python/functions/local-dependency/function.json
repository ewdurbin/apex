{
  "description": "Example with python dependency on an external package that resides locally",
  "hooks":{
    "build": "PYTHONDONTWRITEBYTECODE=1 python -m pip install -r requirements.txt -t .deps",
    "clean": "rm -rf .deps"
  }
}
